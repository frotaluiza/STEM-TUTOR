#!/bin/sh
# post-commit hook — Push automático para remote personal
BRANCH=$(git branch --show-current 2>/dev/null)
if [ -n "$BRANCH" ]; then
  git push personal "$BRANCH" 2>/dev/null
  if [ $? -eq 0 ]; then
    echo "[Auto-Push] OK -> personal/$BRANCH"
  fi
fi
