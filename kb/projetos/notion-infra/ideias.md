# Ideias e Referências — Notion Infrastructure

## 🛠️ Stack e Tecnologias

- "pytest" (the test was run manually via Python CLI)
- ** **OCR.space** (25K requests/mo free) or **Mathcheap.xyz** (unlimited, no signup).
- *Novo passo iniciado*
- 1. First, query the database to check if a row with this Chat ID exists
- </details>
- CNN
- CRUD para notas e blocos
- Limited (Java library + CLI)
- No (Desktop app)
- PIV (Plan -> Implement -> Validate) + Kanban"
- TBD
- Via transformers
- Yes
- Yes (Convert API)
- Yes (Python API + CLI + HTTP service)
- Yes (Python API + CLI + built-in API server)
- Yes (Python API + CLI + server)
- Yes (Python API + CLI)
- Yes (Python API via transformers + vLLM)
- Yes (Python API)
- Yes (REST API)
- Yes (via Mistral platform)
- algum
- asser
- comic
- conflitos.
- ela
- esse
- first for backend - verify the endpoint responds, then test behavior, then edge cases).
- level tools. Since I don't have those as function tools, I could use the Notion API directly via RES
- machine
- node:test | pytest | playwright | tsc --noEmit
- rela

## 💡 Objetivos por Sessão

